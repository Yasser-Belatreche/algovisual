import type { NextPage } from "next";
import { Sorting as SortingPage } from "../../containers/Visualizations/Sorting/Sorting";

const Sorting: NextPage = () => {
  return <SortingPage />;
};

export default Sorting;
