import { render, screen } from "@testing-library/react";
import { Algorithms } from "./Algorithms";

describe("Algorithms Page", () => {
  it("should render correctely", () => {
    // const { container } = render(<Algorithms />);
    // expect(container).toMatchSnapshot();
  });
});
